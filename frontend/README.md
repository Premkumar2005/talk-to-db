# TalkToDB Frontend (React + Vite)

## Setup

```bash
cd frontend
npm install
npm run dev
```

The app will be available at: http://localhost:5173
Make sure the backend (FastAPI) is running at http://localhost:8000.
